---
- name: Converge
  hosts: all
  become: true

  tasks:
    - name: Check if updates still pending
      ansible.builtin.apt:
        name: '*'
        state: latest
        update_cache: true
      check_mode: true
